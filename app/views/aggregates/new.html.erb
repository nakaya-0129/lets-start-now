<%= render "shared/login" %>
<div class="contents">
  <div class="container">
    <h1 class="text-title">成長の記録をつけよう！</h1>
<%= form_with model: @aggregate, url: aggregates_path, class: 'aggregate_user', local: true do |f| %>
      <h3>スタートした日を入力しよう！</h3>
       <div class='field'>
             <%= raw sprintf(
                  f.date_select(
                    :start_day,
                    class:'select-birth',
                    id:"start-day",
                    use_month_numbers: true,
                    prompt:'--',
                    start_year: 1930,
                    end_year: (Time.now.year - 5),
                    date_separator: '%s'),
                  "<span> 年 </span>", "<span> 月 </span>") + "<span> 日 </span>" %>
        </div>
      <div class= "field">
      <h3>取り組んだ期間を入力しよう！</h3>
       <%= f.collection_select(:period_id, Period.all, :id, :name, {}, {class:"select-box",id:"aggregate-category"}) %>
       </div>
       <div class="field">
        <h3>成長を実感した能力はありますか？</h3>
       <%= f.collection_select(:ability_id, Ability.all, :id, :name, {}, {class:"select-box",id:"aggregate-category"}) %>
       </div>
       <div class="field">
        <h3>目標の達成率を記入しよう！(0 ~ 100 )</h3>
        <%= f.text_field :action_point, id: "grow-up", class: "text"%><span>%</span>
      </div>
      <span>成長値:</span>
        <span id= "ability-point"></span>
      <div class="field">
      <h3>成長値を入力して下さい(MAX 10)</h3>
        <%= f.text_field :ability_point,class: "text"%>
      </div>
       <div class="field">
       <h3>自分への褒め言葉を書こう！</h3>
       <%= f.text_area :self_compliment, class: "text"%>
       </div>
      <div class ="actions">
        <%= f.submit "記録する！" ,class: "submit-btn"%>
      </div>
      <% end %>
  </div>
</div>